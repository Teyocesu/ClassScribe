# ClassScribe v0.8.0 — captura confiable, ASR fiel, voces estables y localización

Estado: **CANÓNICA / APROBADA PARA IMPLEMENTACIÓN**

Fecha: 2026-08-24
Alcance: comportamiento y arquitectura de la próxima release; no autoriza por sí solo una implementación.

Esta es la única SPEC de ingeniería de v0.8.0. `PLAN.md` ordena el trabajo y puede cambiar; si discrepan sobre requisitos o invariants, prevalece esta SPEC.

## 1. Objetivo

Hacer que iniciar una clase sea una operación observable, cancelable y acotada; agregar captura explícita de todo el audio de salida; impedir que silencio o ruido se conviertan en texto de otro idioma; y convertir la identificación de voces en un flujo estable, corregible y no destructivo. La misma release separará por completo el idioma de transcripción del idioma de interfaz y localizará el producto en español, inglés y francés.

La experiencia conceptual debe ser común en macOS y Windows. Las implementaciones usarán las APIs nativas de cada sistema y no compartirán abstracciones de captura artificiales.

## 2. Baseline auditado

### 2.1 Producto activo

- Versión actual: `0.7.0`.
- macOS: producto SwiftPM `ClassScribe` en `app/MeetingTranscriber/ClassScribeSources`; helper activo `ClassScribeDiarizer` en `DiarizationHelperSources`. `app/MeetingTranscriber/Sources` es referencia upstream, no el target del producto.
- Windows: aplicación WPF en `app/ClassScribe.Windows`.
- No hay catálogos `.strings`, `.xcstrings` ni `.resx` activos; la interfaz está escrita principalmente en español.
- Las sesiones no tienen schema version explícito. Algunos enums persistidos usan textos visibles en español como representación estable.

### 2.2 Verificación del informe de startup

Los hallazgos previos se verificaron contra el `HEAD` actual (`a0ccc20`); no se adoptaron como premisas sin contrastarlos:

| Hallazgo | macOS | Windows | Conclusión |
|---|---|---|---|
| Preparación ASR no acotada | `AsyncSingleFlight` inicia una tarea detached y conserva la carga aunque el waiter sea cancelado; descarga, compilación y `loadModels` forman una sola espera sin deadline de producto. | La descarga acepta `CancellationToken`, pero tiene timeout HTTP infinito; `WhisperFactory.FromPath` es síncrono y no reporta progreso. | Confirmado. La descarga de FluidAudio sí cancela su `URLSessionTask`; el problema principal de macOS es la propiedad detached de ClassScribe y la carga nativa no cooperativa. |
| Cancelar durante startup | `startClass()` no conserva una tarea/token de startup y `stopClass()` no actúa hasta `isRecording`. | Existe token y botón de cancelación. | Confirmado en macOS; Windows es mejor, pero no puede terminar con garantía una carga nativa síncrona. |
| Callbacks silenciosos considerados sanos | El primer buffer se valida por cantidad de frames y el watchdog sólo observa crecimiento de samples. | El primer paquete no vacío confirma captura. | Confirmado en ambas. |
| Snapshot de árbol de procesos | Los PIDs raíz/helpers se enumeran una sola vez antes de crear el tap. | WASAPI process loopback recibe el árbol del proceso desde el SO, pero la identidad raíz puede caducar/reiniciarse. | Confirmado en macOS; riesgo distinto en Windows. |
| Reintentos ASR indefinidos | 2/4/8/15/30 s y luego 30 s sin abrir circuito. | Reintento cada 9 s sin estado terminal. | Confirmado. |
| Pérdida del vivo durante carga | Ring de 45 s; al expirar el cursor se rebasa a audio reciente. | Ring de 45 s y ventanas recientes de 25 s. | Confirmado. El WAV recupera el final, pero se pierden minutos del vivo. |
| Estado visual prematuro | Tras conectar captura se muestra grabación/transcripción aunque todavía no exista primer resultado ASR. | Tras el primer paquete se muestra “Grabando y guardando”; la distinción ASR no es estructurada. | Confirmado. |

También se verificó que el arranque Core Audio de macOS contiene llamadas nativas síncronas, que un trabajo Core ML cancelado puede seguir ocupando la cabeza de la cola serial y que aumentar timeouts no proporcionaría cancelación real.

### 2.3 Auditoría de idioma y aceptación ASR

#### macOS / Parakeet

- ClassScribe pasa correctamente `TranscriptionLanguage` a `FluidAudio.Language` en vivo y al final.
- FluidAudio 0.15.5 agrupa español, inglés y francés bajo el mismo script latino. `TokenLanguageFilter` separa scripts, no estos tres idiomas.
- Para idiomas latinos no ingleses, el decoder agrega una lista finita de tokens considerados exclusivos del inglés. No es una restricción completa de idioma.
- ClassScribe no ejecuta el VAD de FluidAudio antes de inferencia. El umbral energético existente sólo ayuda a separar párrafos.
- Se acepta cualquier texto no vacío devuelto por Parakeet. Por ello, silencio/ruido ambiguo puede llegar al decoder y explotar el prior inglés del modelo.
- El vocabulario técnico CTC se aplica al procesamiento final; el vivo no usa ese booster.

Conclusión: el idioma llega al motor, pero la implementación no demuestra el invariant para idiomas que comparten script. El defecto no se resuelve con detección posterior de idioma; requiere gating de voz antes de inferencia y aceptación próxima al decoder. Parakeet sólo será conforme si supera el gate de §13; de lo contrario, macOS usará un backend que fuerce realmente el idioma.

#### Windows / Whisper

- `WhisperProcessorBuilder.WithLanguage(languageCode)` fuerza el idioma seleccionado y el prompt técnico se pasa al vivo y al final.
- ClassScribe acepta todo segmento no vacío. No usa VAD, probabilidad de no-speech, log probability ni energía para aceptar el resultado.
- El prompt técnico puede reforzar una alucinación cuando no hay voz; nunca debe sustituir al gate de voz.

Conclusión: la restricción de idioma está configurada, pero falta la mitad de aceptación. Se combinarán VAD/energía con señales nativas que Whisper.net pueda exponer; si el wrapper no expone no-speech/logprob, se agregará la extensión mínima o el VAD previo será la señal primaria documentada.

### 2.4 Auditoría de diarización y hablantes

- En macOS, el label interno `S0` se presenta como `Persona 0`: bug determinista de mapping. Windows suma uno y presenta `Persona 1`.
- Un único cluster puede representar una clase de un hablante o un colapso. Hoy no se persiste evidencia suficiente para distinguirlos.
- La asignación ASR↔speaker usa mayor solapamiento temporal. Sin solapamiento pero con spans, elige el speaker más cercano con confianza `0.25`; sin spans conserva unknown.
- La “confianza” es principalmente cobertura temporal, no calidad de clustering. Windows asigna calidad `1` a todos los spans, por lo que no es comparable.
- Los IDs persistidos son labels efímeros del motor y pueden cambiar al reprocesar.
- Reprocesar reconstruye speakers, profesor y review; puede borrar correcciones manuales.
- macOS guarda embeddings/referencias por materia con umbral `0.62`, sin margen contra el segundo candidato ni versionado del modelo. Windows no devuelve embeddings desde el worker ni usa referencias conocidas.
- Seleccionar profesor y reasignar un item de revisión existen; rename, merge y reassign/split general no.
- El transcript completo suele persistirse antes de diarizar, pero no hay un modelo explícito “ASR original inmutable + anotaciones humanas”.

Conclusión: `Persona 0` tiene al menos una causa determinista que debe corregirse antes de experimentar con ML. Un fallo no puede sintetizar un speaker. Un único cluster no se declarará fallo sólo por cardinalidad; será posible colapso únicamente con evidencia contradictoria.

## 3. Scope

### Incluido

1. Startup por fases, observable y cancelable.
2. Captura online de una aplicación o de todo el audio de salida, siempre elegida explícitamente.
3. Clasificación: sin callbacks, callbacks silenciosos, audio audible sin voz y voz.
4. ASR supervisado, con progreso, prewarm seguro y degradación a grabación sin ASR.
5. VAD/aceptación próximos a inferencia, restricción de idioma y vocabulario técnico.
6. Identidad estable de speakers, diagnósticos, correcciones no destructivas y UX de review.
7. Idioma de interfaz `system|es|en|fr`, independiente del de transcripción.
8. Localización nativa de UI y documentación de usuario en tres idiomas.
9. Schema versionado y lectura de todas las sesiones recuperables hoy.
10. Tests de fallos materiales y gates físicos reproducibles.

### Fuera de scope

- Cloud, cuentas, telemetría, traducción o envío de audio/texto.
- Separación de fuentes o reconstrucción de voces solapadas mediante ML experimental.
- Identidad biométrica global; las referencias siguen locales y ligadas a una materia.
- Abstracciones internas artificialmente idénticas entre APIs de captura.
- Rediseño visual general, colaboración o sincronización entre equipos.
- Activar captura global automáticamente.
- Traducir SPEC, PLAN, arquitectura o handoffs internos.

## 4. Invariants de release

1. Toda espera de una clase tiene owner, cancelación, deadline de producto y resultado terminal.
2. Cancelar startup detiene captura, libera recursos y evita que callbacks tardíos reactiven la sesión. Una operación nativa no cooperativa no bloquea UI, stop ni otra sesión.
3. “Transcribiendo” sólo aparece tras aceptar el primer resultado ASR. “Audio recibido” no implica “voz” ni “ASR listo”.
4. Si ASR no queda listo o abre circuito, la captura durable continúa en `grabandoSinASR`; no reintenta por siempre.
5. Ningún atraso elimina audio: la línea durable es fuente de verdad y el ring sólo caché.
6. Captura global requiere acción y aceptación explícitas; nunca es fallback silencioso.
7. El master global conserva sample rate, canales y precisión razonables; 16 kHz mono es sólo derivado ASR.
8. El idioma seleccionado llega al backend y restringe la salida. Silencio/ruido sin voz aceptada no produce texto.
9. No se aplican filtros posteriores “parece inglés, borrar”.
10. Fallo de diarización produce `unknown/review`, nunca `Persona 0` ni speaker inventado.
11. IDs de speaker son estables en la sesión y distintos del nombre visible y cluster efímero.
12. Una corrección humana prevalece sobre reprocesamiento; un resultado nuevo es propuesta, no overwrite.
13. Transcript ASR completo original y master se conservan siempre.
14. Una referencia sólo asigna identidad con threshold, calidad y margen versionados/calibrados.
15. Idioma de interfaz e idioma de transcripción son settings separados.
16. Toda sesión histórica recuperable por v0.7 sigue siendo recuperable; una migración no borra artefactos.

## 5. UX y modelo de estado

Captura y ASR avanzan en paralelo; no se modelarán en un enum monolítico.

`CapturePhase`: `idle`, `validatingSource`, `connecting`, `waitingForFrames`, `framesSilent`, `audioAudible`, `recording`, `stopping`, `failedRecoverable`, `failedTerminal`.

`AsrPhase`: `idle`, `preparingDownload`, `preparingLoad`, `waitingForSpeech`, `transcribing`, `retryScheduled`, `unavailableForSession`, `failedRecoverable`.

`SessionPhase` cubre el lifecycle durable: `draft`, `starting`, `recording`, `stopping`, `processing`, `complete`, `cancelled`, `recoverable`, `failed`. Ninguno se persiste como texto localizado.

La UI deriva mensajes de la combinación y muestra como mínimo: validando fuente, conectando captura, esperando audio, audio recibido, preparando modelo, transcribiendo, grabando sin ASR y error recuperable.

### Progreso

- Descarga: bytes transferidos/esperados, fase y cancelación; indeterminado si no hay total.
- Carga/compilación: fase explícita e indeterminada salvo unidades reales; no inventar porcentajes.
- Prewarm: separado de startup; no retrasa captura ni oculta descarga.
- Captura: fuente/scope, callbacks, RMS/peak y presencia de voz.

### Acciones recuperables

- Sin callbacks: revisar/actualizar/reintentar/otra app y `Capturar audio del equipo`.
- Callbacks silenciosos: explicar conexión sin señal; esperar, reproducir audio, reintentar o global.
- Audio audible sin voz: grabar y mostrar “audio recibido, esperando voz”; no es error automático.
- ASR no disponible: mantener stop/export/audio, permitir retry y reproceso posterior.
- Global siempre abre confirmación de alcance antes de empezar/reiniciar captura.

## 6. Arquitectura de startup y ASR

### Ownership y cancelación

Cada intento obtiene `SessionAttemptID`, token raíz y componentes hijos. Toda transición/callback incluye ese ID; resultados viejos se descartan. Cancelar invalida la generación, cancela waits/descarga/backoff, detiene captura y escritura, termina el worker ASR si era propiedad de la clase, persiste estado coherente y devuelve UI sin esperar una llamada nativa no cooperativa.

No cuenta como cancelación real envolver una llamada síncrona en `Task` y dejarla en una cola compartida.

### Boundary supervisado para ASR

Carga e inferencia nativas se ejecutarán en worker supervisado y reiniciable:

- macOS: helper firmado con protocolo versionado por pipes/XPC; aloja FluidAudio/Core ML.
- Windows: helper .NET/native con protocolo versionado por named pipes/stdio; aloja Whisper.net/whisper.cpp.

El supervisor termina el proceso en cancelación, deadline, crash o falta de heartbeat. Así Core ML/Whisper no bloquea stop o la clase siguiente. El worker recibe paths ya validados y audio derivado, y devuelve progreso/resultados tipados.

La descarga se separa de carga, es cancelable/reanudable y conserva integridad. En macOS se aprovechan la cancelación y parciales de FluidAudio, eliminando la propiedad detached actual. Windows conserva SHA-256/tamaño. Ambas plataformas tienen deadline absoluto y de inactividad, reintentos limitados, backoff cancelable, parciales seguros, progreso y resultado terminal.

### Presupuestos, no timeouts gigantes

Cada fase tiene presupuesto configurable/testeable. Al agotarlo: conexión sin callbacks → error recuperable; silencio → advisory; modelo no listo/worker sin heartbeat → `unavailableForSession`; retries agotados → circuito abierto hasta acción del usuario/nueva sesión. Cambiar un número no puede ser la única corrección de un componente no cancelable.

### Precalentamiento

- Con modelos íntegros, preparar worker en idle después de abrir UI.
- Descarga nunca sorpresiva: consentimiento/configuración de red/almacenamiento, progreso y cancelación en ajustes.
- Prewarm tiene owner de aplicación, baja prioridad y presupuesto propio; no comparte tarea incontrolable con startup.
- Se descarta bajo presión, cambio de modelo, inactividad o crash.
- Captura inicia sin prewarm; el backlog durable permite recuperar o procesar al final.

### Línea de tiempo

Master durable + journal de spans son fuente de verdad. Cada chunk tiene tiempo monotónico y posición durable. ASR atrasado puede leer disco y procesar backlog con límite; si no alcanza tiempo real abre el circuito vivo y deja el final para reproceso. Nunca rebasa silenciosamente el cursor.

## 7. Captura y fallback global

### Comportamiento común

`CaptureSource.scope`: `microphone|application|systemOutput`. Metadata: tipo, nombre, identidad estable, PID inicial sólo diagnóstico, formatos nativo/master/ASR y gaps. PIDs no son identidad durable.

La señal se clasifica en dos capas: transporte (sin callback vs frames) y contenido (silencio/noise floor, audible sin voz, voz). RMS/peak detecta señal/clipping; VAD detecta voz. Silencio legítimo genera advisory, nunca cambio automático o error terminal.

### macOS

- Aplicación: `CATapDescription` por procesos, excluyendo ClassScribe.
- Global: `CATapDescription(stereoGlobalTapButExcludeProcesses:)`, excluyendo ClassScribe/helpers.
- Reenumerar identidad bundle/ejecutable al validar, justo antes del tap, ante lifecycle y al entrar en no-callbacks/silencio inesperado. Reconciliar o reiniciar tap con generation ID y registrar gaps.
- Chrome/Teams/Zoom: validar PIDs realmente traducidos a Core Audio, no sólo POSIX.
- Setup sale de `MainActor`. Si fault injection prueba que ignora cancelación, captura online pasa a helper supervisado antes de release; no se aceptan threads huérfanos por intento.

### Windows

- Aplicación: WASAPI Application Loopback con `IncludeTargetProcessTree`; revalidar identidad de ejecutable/package y reconstruir ante root reemplazado.
- Global: WASAPI loopback del render endpoint seleccionado/default. Registrar cambios; no saltar silenciosamente de dispositivo.
- No depender sólo de ventana visible para una identidad elegida; refrescar al fallar/no recibir audio.
- Cancelar activación, primer paquete, escritura y teardown. Si una llamada nativa no coopera, aplica el mismo gate de aislamiento.

### Fidelidad

- El master no será 16 kHz mono por conveniencia.
- Para app/global, conservar sample rate/canales del mix hasta estéreo y precisión PCM razonable; registrar conversión.
- Derivar 16 kHz mono sólo para VAD/ASR, sin necesidad de duplicar el master.
- `source.wav` legacy 16 kHz mono sigue válido.
- Probar clases largas/límite RIFF 4 GiB antes de elegir RF64/W64 o segmentos manifestados; nunca truncar.

### Consentimiento/privacidad

La confirmación global enumera notificaciones, otras apps, navegador, reproductores y sistema; muestra dispositivo y exclusiones. El usuario confirma inequívocamente. Se registra localmente versión de copy/timestamp como evidencia UX, no como permiso para futuros cambios automáticos. Cancelar mantiene la fuente anterior.

## 8. Voz, idioma y aceptación

Pipeline común:

1. Capturar master.
2. Derivar 16 kHz mono preservando timestamps.
3. Medir RMS/peak/clipping.
4. VAD con hysteresis, duración mínima y padding calibrados.
5. Enviar sólo ventanas con evidencia de voz, conservando contexto.
6. Configurar idioma antes de decoder.
7. Evaluar no-speech, logprob, entropy/compression, confidence y estabilidad disponibles.
8. Aceptar/diferir/rechazar antes de incorporar al transcript.

Energía no es VAD; VAD no certifica texto. Thresholds son por backend y fixtures. Métricas van a diagnostics, no al texto visible.

### macOS

- Usar `VadManager`/Silero de FluidAudio con estado streaming/config versionada.
- Mantener `FluidAudio.Language` en toda inferencia.
- El filtro de script no cuenta como restricción es/en/fr. Parakeet queda sólo si VAD + aceptación + decoder superan la suite.
- Si falla, usar backend local con forced-language real o mejora upstream mantenible; no lista casera de palabras.
- Vocabulario CTC sólo rescora con voz aceptada y se prueba vivo/final.

### Windows

- Mantener `WithLanguage(es|en|fr)` y transcripción, no autodetección.
- VAD local antes de Whisper; sherpa-onnx Silero es compatible con el stack.
- Exponer no-speech/logprob cuando sea posible; si Whisper.net no lo proyecta, extensión mínima o VAD previo como señal primaria.
- Prompt técnico sólo con voz; probar mejora sin falsos positivos.

### Gate de idioma

Corpus pequeño/versionado por idioma: silencio digital; room tone/ventilador/clicks/música/notificaciones; voz elegida con/sin vocabulario; voz clara en otro idioma; voz débil/pausas/fronteras. Falla si no-speech produce frase, el backend ignora sistemáticamente idioma o vocabulario aumenta falsos positivos. Code-switching real se prueba aparte.

## 9. Diarización, identidad y correcciones

### Modelo canónico

- `SpeakerID`: UUID estable de ClassScribe por sesión.
- `EngineClusterID`: label efímero diagnóstico; nunca visible ni identidad durable.
- `displayName`: editable; default localizado (`Persona 1`, `Speaker 1`, `Personne 1`) sin cambiar ID.
- `identityState`: `unknown|candidate|confirmed`; `role`: `unknown|professor|participant`.
- `DiarizationProposal`: corrida inmutable con versiones, spans, clusters, embeddings/centroides y métricas disponibles.
- `SpeakerCorrectionOverlay`: operaciones humanas versionadas sobre IDs/segmentos.
- `SegmentID`: UUID persistido, no índice. Resegmentación reconcilia por tiempo/evidencia y manda conflictos a review.

### Precedencia

1. Corrección humana.
2. Referencia conocida que supera gate completo.
3. Propuesta actual.
4. `unknown/review`.

Reprocesar crea propuesta y la compara con overlay; nunca reemplaza rename, merge, reassign o professor confirmado. Sólo una acción explícita descarta una corrección.

### Fallos/colapso

- Cero spans, crash, timeout, output inválido o cobertura insuficiente → unknown/review.
- Un cluster válido se muestra como Persona 1/equivalente, nunca Persona 0.
- Un cluster no es fallo solo. `possibleClusterCollapse` exige evidencia contradictoria (referencias distintas, embeddings separables, discontinuidades/distancias calibradas).
- Si una plataforma no expone calidad/embeddings, declara `unavailable`, no `1`.
- Separar cobertura temporal, calidad cluster y confianza de identidad; no ocultarlas en un número.

### Referencias conocidas

Registrar ID local, materia, backend/model/version, dimensión, normalización, duración, calidad y fecha. No comparar incompatibles. Exigir calidad/duración, score calibrado, margen sobre segundo candidato y no contradicción humana. Windows devuelve embeddings compatibles o declara función no disponible; no finge paridad.

### UX

- Rename cambia displayName, no ID.
- Merge reversible mapea IDs a canónico conservando origen.
- Reassign mueve segmentos a speaker/unknown.
- Split sólo en límites de segmento/VAD/timestamp; no promete separar overlap.
- Mark professor es corrección explícita de máxima precedencia.
- Review cubre baja calidad/cobertura, overlap, unknown, conflicto o posible colapso.
- El original completo y cada propuesta siguen accesibles.

## 10. Localización

### Settings

- `transcriptionLanguage=es|en|fr`, por sesión y opcionalmente último valor de creación.
- `interfaceLocale=system|es|en|fr`, setting persistido de aplicación.
- `system` sigue el SO soportado con fallback documentado.
- Cambiar UI no cambia sesión, modelo ni idioma ASR.

### macOS

String Catalog único; `LocalizedStringResource`/`String(localized:)` también en errores/accesibilidad; locale en root environment para refresh. Reconstruir componentes AppKit que no reaccionen sin reiniciar app. Persistir fuera de metadata de clase.

### Windows

`.resx` neutrales + satélites `es/en/fr`; `ResourceManager` y `CurrentUICulture`/`DefaultThreadCurrentUICulture`; bindings notificables o reconstrucción de ventana conservando ViewModel. Sin cientos de `if language`.

### Inventario y docs

Cubrir botones, estados, errores, tooltips, accesibilidad, diálogos, historial, exportación, onboarding, idiomas, captura, ASR y diarización. Tokens de dominio estables se localizan al presentar.

`README.md`, `README.en.md`, `README.fr.md` se enlazan. Sólo docs realmente de usuario (p. ej. guía Windows) se traducen. SPEC/PLAN/arquitectura/handoff quedan únicos.

## 11. Persistencia y compatibilidad

### Schema v2 aditivo

`schemaVersion: 2` y tokens no localizados. Registra session phase, idioma ASR, capture scope, formatos, versiones backend/model/config, referencia a ASR original, proposals/overlay y gaps/diagnostics sin datos sensibles innecesarios. Idioma UI queda fuera. Escritura atómica; no borrar/reemplazar antes de validar.

### Legacy

- Sin idioma → español.
- `online` → `application`; `inPerson` → `microphone`.
- Estados raw español/aliases Windows → tokens v2.
- Labels legacy generan `SpeakerID` determinista dentro de sesión; Persona 0 se importa como nombre legacy, no evidencia.
- Sin spans válidos → unknown aunque haya label viejo.
- Referencias sin model/version/dimensión se conservan, pero no se usan automáticamente.

Abrir no reescribe. Primera modificación migra atómicamente y mantiene recuperables audio/metadata/exports. ASR original es append-only/inmutable por corrida; edición/speakers son overlays/proposals. Exporters resuelven vistas sin destruir fuentes.

## 12. Privacidad y seguridad

- Local, sin nueva telemetría/servicio remoto.
- Descargas de orígenes fijados con integridad previa a carga.
- Helpers: protocolo versionado, validación de paths/longitudes, mínimo privilegio, límites de mensajes, heartbeat/lifecycle.
- Global excluye ClassScribe/helpers si la API permite y muestra scope durante grabación.
- Logs sin audio, transcript, embeddings o materia por defecto; usar IDs opacos/métricas.
- Referencias locales y eliminables.
- UI/docs recuerdan consentimiento legal; global no lo modifica.

## 13. Acceptance criteria

### Startup/captura

- Estados estructurados; no `transcribing` antes del primer resultado aceptado.
- Cancelar cada fase cierra captura y evita transiciones tardías.
- Ningún wait de callback/download/load/worker/retry sin cancelación y terminal.
- Modelo agotado → recordingWithoutAsr, sin retry infinito.
- Atraso >45 s no elimina audio ni mueve cursor silenciosamente.
- Cambio helper/PID macOS y root Windows se detectan/reconcilian.
- No callbacks, silencio, audible y voz dan UX distinta.

### Global/privacidad

- macOS mix global excluyendo self; Windows render endpoint WASAPI.
- Master no forzado a 16 kHz mono.
- CTA aparece tras fallo/no señal por aplicación.
- Global nunca activa sin confirmación; cancelarla no cambia fuente.

### Idioma/ASR

- Idioma llega a backend vivo/final.
- Silencio/ruido no produce segmentos aceptados.
- Voz es/en/fr respeta configuración; otro idioma detecta backend que ignora selección.
- Vocabulario mejora objetivo sin falsos positivos.
- Parakeet no se declara conforme sólo por filtro de script.

### Speakers

- Nunca Persona 0 como fallback.
- Fallo/timeout/vacío → unknown/review preservando transcript.
- Dos voces distinguibles no colapsan silenciosamente; ambiguo va a review.
- IDs estables y correcciones sobreviven save/load/reprocess.
- Rename/merge/reassign/professor/review son reversibles o confirmados.
- Referencias incompatibles/bajo gate no asignan.

### Localización/compatibilidad

- Keysets completos y sin strings visibles fuera de recursos salvo allowlist.
- `system|es|en|fr` persiste/cambia sin restart en flujos cubiertos.
- UI locale y ASR language no se alteran entre sí.
- Golden sessions v0.7 macOS/Windows abren, exportan y reprocesan sin borrado.
- Los README se enlazan y no se triplica ingeniería.

## 14. Tests mínimos

Desarrollo `focused → subsystem`; sólo duplicar niveles si protegen fallos distintos.

| Failure mode | Nivel mínimo |
|---|---|
| Sin callbacks | unit coordinador/fake clock + integración adapter |
| Cancel por fase | unit determinista + integración worker/capture |
| PID cambia | unit reconciliación + integración específica por SO |
| Silencio/audible/voz | unit DSP/VAD con fixtures + pipeline |
| Modelo colgado/crash | integración supervisor/worker |
| Idioma + silencio/ruido | fixture por backend/idioma |
| Ring expira | integración timeline con ASR lento |
| Speaker mapping/collapse/failure | unit assignment + fixture diarization |
| Corrección sobre reproceso | round-trip persistence |
| Global sin consentimiento | state-machine enumerando transiciones |
| Localización | igualdad de keysets + detector de literals allowlisted |
| Legacy | golden fixtures v0.7 de ambas plataformas |

Tests de tiempo usan reloj inyectado; no duermen minutos. Fixtures de audio tienen procedencia/licencia.

## 15. Gates físicos/manuales

### macOS

1. TCC limpio/denegado/aceptado para audio y micrófono.
2. Chrome Meet creando/cerrando helpers; verificar reenumeración y aislamiento por app.
3. Teams/Zoom con root reiniciado y audio tardío.
4. Global con dos apps + notificación; confirmar alcance y que cancelar no captura.
5. Cambiar output device; verificar estado/gap y no cambio silencioso.
6. Primera descarga/load lento/cancel/worker crash; la clase siguiente inicia.
7. Inspeccionar sample rate/canales/duración master y derivado separado.

### Windows físico

1. Application loopback en Chrome/Teams/Zoom con child/root reemplazado.
2. System loopback con dos apps, notificación, endpoint change y consentimiento.
3. Primera descarga/cancel/load lento/worker crash y recuperación.
4. Micrófono/device unplug/permisos desktop.
5. Fidelidad y clase larga próxima al límite de contenedor.

### Ambas

1. Silencio, room tone, clicks, notificación, música y voz débil por idioma.
2. Clase consentida de dos speakers con turnos/overlap; IDs antes/después de reproceso.
3. Clase legítima de uno para evitar falso collapse.
4. Rename/merge/reassign/professor, cerrar/abrir/reprocesar; overlay intacto.
5. Cambiar UI locale durante captura/error/history/dialog; estado/transcript intactos.

Registrar SO, hardware, dispositivo, app fuente, backend/modelos, pasos, resultado y artefactos no sensibles. Un gate no ejecutado queda pendiente, nunca aprobado por inferencia.

## 16. Estrategia multiplataforma

| Requisito | Común | macOS | Windows | Diferencia justificada |
|---|---|---|---|---|
| Captura app | Scope/señal | CATap + reenumeración | Application Loopback + árbol SO | Lifecycle PID/API distintos |
| Global | Consentimiento/master fiel | Stereo global CATap | Render endpoint WASAPI | Mix por procesos vs endpoint |
| Cancel ASR | Supervisor/circuit breaker | Helper FluidAudio/Core ML | Helper Whisper native | Backends distintos |
| VAD | Gate previo | FluidAudio Silero | sherpa Silero | Stacks existentes |
| Speakers | IDs/proposals/overlay | FluidAudio embeddings | extender worker sherpa | Windows hoy no expone embeddings |
| Localización | claves/live refresh | String Catalog | `.resx` | Mecanismos nativos |

## 17. Riesgos y decisiones abiertas

### Riesgos

1. Parakeet fijado no distingue es/en/fr por filtro general; VAD corrige no-speech, no necesariamente voz real en otro idioma.
2. Helpers agregan IPC/firma/packaging, pero son la frontera fiable de cancelación nativa.
3. Browsers/meeting apps macOS cambian topología; restart de tap puede crear gaps visibles.
4. Master fiel aumenta almacenamiento y puede superar RIFF 4 GiB.
5. Reprocesar cambia segmentación/clusters; reconciliar overlays requiere conflictos explícitos.
6. Embeddings no son comparables cross-version; referencias legacy deben quedar inactivas.
7. WPF no refresca automáticamente todos los bindings al cambiar culture.

### Decisiones abiertas no bloqueantes para empezar

1. Backend macOS: Parakeet sólo si supera gate o backend forced-language.
2. Master largo: RF64/W64 vs WAV segmentado + manifest.
3. IPC: XPC/pipes macOS; named pipes/stdio Windows.
4. Locale de upgrade: `system` o preservar español una vez en instalaciones existentes.
5. Thresholds VAD/no-speech/embeddings, exclusivamente con fixtures y versionados.
6. Mover captura a helper si el spike físico demuestra setup no cancelable.

No hay pregunta bloqueante para iniciar infraestructura/caracterización. Antes de cerrar ASR será bloqueante elegir backend macOS con evidencia; antes de persistir masters, elegir contenedor tras clase larga.

## 18. Referencias primarias

- Apple, global stereo tap: <https://developer.apple.com/documentation/coreaudio/catapdescription/init(stereoglobaltapbutexcludeprocesses:)>
- Apple, String Catalogs: <https://developer.apple.com/documentation/xcode/localizing-and-varying-text-with-a-string-catalog>
- Apple, SwiftUI locale: <https://developer.apple.com/documentation/swiftui/environmentvalues/locale>
- Microsoft, WASAPI loopback: <https://learn.microsoft.com/en-us/windows/win32/coreaudio/loopback-recording>
- Microsoft, Application Loopback sample: <https://learn.microsoft.com/en-us/samples/microsoft/windows-classic-samples/applicationloopbackaudio-sample/>
- Microsoft, WPF localization: <https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/wpf-globalization-and-localization-overview>
- Microsoft, `ResourceManager`: <https://learn.microsoft.com/en-us/dotnet/api/system.resources.resourcemanager>
- OpenAI Whisper: <https://github.com/openai/whisper>
- whisper.cpp language/no-speech/logprob: <https://github.com/ggml-org/whisper.cpp/blob/master/include/whisper.h>
- sherpa-onnx VAD: <https://k2-fsa.github.io/sherpa/onnx/c-api/html/vad.html>
- FluidAudio 0.15.5 auditado: <https://github.com/FluidInference/FluidAudio/tree/19600a485baa4998812e4654b70d2bab8f2c9949>
